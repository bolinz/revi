# {{project_name}}

{{project_description}}

## Template

- Kind: `{{template_kind}}`
- Runtime: `{{default_runtime}}`
- Version: `{{project_version}}`

## Git Workflow

- Stable branch: `main`
- Feature branches: `feat/<name>`
- Bugfix branches: `fix/<name>`
- Release blockers: `hotfix/<name>`
- Release tags: `vX.Y.Z`

## Commands

- Install: `{{commands.install}}`
- Start: `{{commands.start}}`
- Validate: `{{commands.validate}}`
- Release prep: `{{commands.release_prep}}`

## Local Checks

```bash
./gradlew check
./gradlew build
```

## Release

- Binary artifact publication via GitHub Releases
- Native image publication for multiple platforms

Release checklist:
1. Merge work back to `main`
2. Update version and `CHANGELOG.md`
3. Tag `{{release_tag_example}}`
4. Push `main` and the tag

## Next Steps

1. Add CLI argument parsing using `kotlinx-cli`
2. Configure native image compilation if needed
3. Update `README.md` and `CONTRIBUTING.md` with stack-specific details
