# {{project_name}}

{{project_description}}

## Template

- Kind: `{{template_kind}}`
- Runtime: `{{default_runtime}}`
- Version: `{{project_version}}`

## Git Workflow

- Stable branch: `main`
- Feature branches: `feat/<name>`
- Bugfix branches: `fix/<name>`
- Release blockers: `hotfix/<name>`
- Release tags: `vX.Y.Z`

## Commands

- Install: `{{commands.install}}`
- Start: `{{commands.start}}`
- Validate: `{{commands.validate}}`
- Release prep: `{{commands.release_prep}}`

## Local Checks

```bash
go vet ./...
go test ./...
```

## Release

- Container image publication via GHCR-compatible registry
- GitHub Release notes for deployment guidance

Release checklist:
1. Merge work back to `main`
2. Update version and `CHANGELOG.md`
3. Tag `{{release_tag_example}}`
4. Push `main` and the tag

## Next Steps

1. Add database driver if needed (e.g., `pgx`, `sqlx`)
2. Configure deployment target and environment variables
3. Update `README.md` and `CONTRIBUTING.md` with stack-specific details
